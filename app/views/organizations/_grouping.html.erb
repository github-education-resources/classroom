<li class="clearfix">
  <%= link_to 'Edit', edit_organization_grouping_path(@organization, grouping.id), class: 'btn btn-sm right' %>
  <span class="octicon octicon-organization"></span>
  <strong><%= link_to grouping.title, organization_grouping_path(@organization, grouping.id) %></strong>
</li>
