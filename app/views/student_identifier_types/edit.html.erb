<%= render 'organizations/organization_banner' %>

<div class="site-content">
  <div class="site-content-cap">
    <h2 class="site-content-heading">Edit identifier type</h2>
  </div>
</div>

<div class="site-content-body">
  <div class="flash flash-with-icon flash-form">
    <%= octicon 'alert' %>
    Please do not ask students to provide any confidential information (e.g. SSN or bank account number) as their identifiers.
  </div>
  <%= form_for [@organization, @student_identifier_type] do |f| %>
      <%= render partial: 'student_identifier_types/form_options', locals: { f: f } %>
      <div class="form-actions">
        <%= f.submit 'Save changes', class: 'btn btn-green' %>
        <%= link_to 'Cancel', organization_student_identifier_types_path(@organization), class: 'btn', role: 'button'%>
      </div>
  <% end %>
</div>
