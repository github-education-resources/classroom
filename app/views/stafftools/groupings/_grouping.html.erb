<div class="classroom-resource-item">
  <span class="assignment-icon assignment-icon-group left">
    <%= octicon 'organization', height: 22 %>
  </span>

  <%= link_to stafftools_grouping_path(grouping.id) do %>
    <strong class="search-result-title"><%= grouping.title %></strong>
  <% end %>

  <span class="text-muted css-truncate css-truncate-target">Created <%= local_time_ago(grouping.created_at) %></span>
</div>
