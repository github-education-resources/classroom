<li class="clearfix">
  <%= image_tag organization.avatar_url(40), height: 20, width: 20, alt: "@#{organization.login}" %>
  <strong><%= link_to organization.slug, stafftools_organization_path(organization.id) %></strong>
</li>
