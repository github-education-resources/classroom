#!/bin/sh

# bin/docker/wait-for-dependencies: Wait for all of the required services
# before exiting.

set -e

cd "$(dirname "$0")/../.."

# Elasticsearch

# Postgres

# Memcached

# Redis

echo "Nailed it"
